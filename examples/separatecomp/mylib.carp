(load-static "out/sublib.carpi")

(Project.config "title" "mylib")

(defmodule MyLib
  (deftype Vector2 [x Int, y Int])

  (defn ident [x] x)
  (defn add1 [x] (Int.+ x SubLib.one))
  (defn isodd? [x] (Int.odd? x))

  (defmacro macadd1 [x] (list 'add1 x))

  (def ban (SubLib.DumbEnum.Banana))
  )
