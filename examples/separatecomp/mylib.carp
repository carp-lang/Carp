(relative-include "my_int.h")
(load-static "out/sublib.carpi")

(Project.config "title" "mylib")

(defmodule MyLib
  ;; (deftype DumbEnum
  ;;   Apple
  ;;   Banana
  ;;   Pear)

  (register + (λ [Int Int] Int))
  (defn ident [x] x)
  (defn add1 [x] (+ x SubLib.one))

  (defmacro macadd1 [x] (list 'add1 x))
  )
