(import gl)

(defn tick [state]
  (+ state 0.033f))

(defn draw [state]
  (let [r 0.9f
        t (copy state)]
    (draw-line 0f
               0f
               (* r (cosf t))
               (* r (sinf t)))))

(defn spin []
  (glfw-app "This is a demo" default-init tick draw default-on-keys))
