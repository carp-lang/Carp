(use IO)
(use Int)
(use Array)

(deftype A [s String])

(defn f []
  (let [stuff [(A.init (String.copy "hej")) (A.init (String.copy "svej"))]]
    ()))

(defn main []
  (do (f)
      (println "OK.")))
