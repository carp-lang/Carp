
(defn greatest [xs ys]
  (let [x-sum (count xs)
        y-sum (count ys)]
    (if (< x-sum y-sum)
      (println (copy "ys are bigger"))
      (println "xs are bigger"))))

(defn try-it []
  (greatest &[1 2 3] &[5 6]))
