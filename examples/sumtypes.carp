(use Maybe)

;; This is a bug right now because the 'x' variable:
(defn confusion [x]
  (match x
    (Maybe.Just x) x
    (Maybe.Nothing) @"Nope"))

(defn main []
  (println* (confusion (Just @"Yo"))))
