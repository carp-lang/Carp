;; (Debug.sanitize-addresses)
;; (Project.config "print-ast" true)

;; (defn main []
;;   (let [lam (fn [x] (let [gah (fn [] 3.3f)]
;;                       (* 10 x)))]
;;     (println* (+ (lam 3) (lam 4)))))

;; (defn f [y]
;;   (fn [x] (Int.+ x y)))

(def glob 300)

(defn f [x]
  (let [a 1]
    (fn [y]
      (let [b 2]
        (+ (+ (+ (+ glob y) x) a) b)))))

(defn-do main []
  (let [g (f 5)]
    (println* (g 3))))
