(use IO)
(use Int)
(use Float)
(use Double)
(use Array)
(use System)
(use String)
(use Char)
(use Bool)

(defn f [func r]
  (let [s (copy r)]
    (func (ref s))))

(defn main []
  (f println "hej"))

