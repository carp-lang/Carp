(use IO)
(use Int)
(use Float)
(use Double)
(use Array)
(use System)
(use String)
(use Char)

(defn exclaim [x] (String.append x @"!"))

(defn main []
  (let [stuff (map exclaim (replicate 2 @"Yo"))]
    (println &(Array.str &stuff))))
