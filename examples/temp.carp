(Debug.sanitize-addresses)
(Project.config "print-ast" true)

;; Syntax D
(deftype (Maybe a)
  (Just [a])
  (Nothing []))

(deftype Cool (a [Int]) (b [Bool Bool]))

(defmodule Cool
  (defn f [] 123))

;; (defn f []
;;   ())

;; (deftype (Blah t)
;;     [x Int])
