(import IO)
(import Int)
(import Float)
(import Double)
(import Array)
(import System)
(import String)
(import Char)

(defn square [x]
  (Int.* x x))

(defn main []
  (let [stuff [1 2 3 4 5]
        after (emap square stuff)]
    (println (refstr &stuff))))
