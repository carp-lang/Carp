(import IO)
(import Int)
(import Float)
(import Double)
(import Array)
(import System)
(import String)
(import Char)

(defn first [x]
  (nth (ref x) 0))

(defn f []
  (first &[10 20 30]))

(defn main []
  (println (refstr (f))))
