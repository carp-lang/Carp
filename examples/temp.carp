;; (use IO)
;; (use Int)
;; (use Float)
;; (use Double)
;; (use Array)
;; (use System)
;; (use String)
;; (use Char)
;; (use Bool)

(defn f []
  (let [thing @"THING"
        xs [&thing]] ;; <- should fail!
    xs))

(defn main []
  (IO.println (ref (Array.str &(f)))))
