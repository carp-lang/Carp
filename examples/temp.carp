;; (use IO)
(use Int)
;; (use Float)
;; (use Double)
;; (use Array)
;; (use System)
;; (use String)
;; (use Char)
;; (use Bool)

(defn reducer [x y]
  (Int.+ @x @y))

(defn f [xs]
  (Array.reduce reducer 0 xs))
