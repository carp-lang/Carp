(system-include "carp_int.h")

(defmodule Int
  (Numeric.register Int)
  (Integral.register Int)
  (defn zero []
    0)
)

(defmodule IntRef
  (defn = [a b]
    (Int.= @a @b))

  (defn < [a b]
    (Int.< @a @b))

  (defn > [a b]
    (Int.> @a @b))

  (defn + [a b]
    (Int.+ @a @b))

)
