(system-include "carp_long.h")

(defmodule Long
  (Numeric.register Long)
  (Integral.register Long)
)

(defmodule LongRef
  (defn = [a b]
    (Long.= @a @b))

  (defn < [a b]
    (Long.< @a @b))

  (defn > [a b]
    (Long.> @a @b))
)
