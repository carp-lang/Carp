(system-include "carp_short.h")

(defmodule Short
  (register-type Short)
  (Numeric.register Short)
  (Integral.register Short)
  (register str (λ [Short] String))
)

(defmodule ShortRef
  (NumericRef.register Short)
)
