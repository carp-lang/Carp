(Project.config "file-path-print-length" "short")

; set! where the ref source is a copied string that goes out of scope.
(defn main []
  (let-do [x ""]
    (let [s @"ephemeral"]
      (set! x &s))
    (println* x)))
